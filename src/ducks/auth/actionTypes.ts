const prefix = 'AUTH/'

const AUTH_IS_AUTH = `${prefix}IS_AUTH`
const AUTH_NOT_AUTH = `${prefix}NOT_AUTH`
const AUTH_LOGIN = `${prefix}LOGIN`
const AUTH_SUCCESS = `${prefix}SUCCESS`
const AUTH_FAILED = `${prefix}FAILED`
const AUTH_REGISTRATION = `${prefix}REGISTRATION`
const AUTH_LOGOUT = `${prefix}LOGOUT`

export {
  AUTH_IS_AUTH,
  AUTH_NOT_AUTH,
  AUTH_LOGIN,
  AUTH_REGISTRATION,
  AUTH_SUCCESS,
  AUTH_FAILED,
  AUTH_LOGOUT,
}
